//DECLARATIVE
pipeline {
	agent any
	stages { 
		stage('build') {
			steps {
				echo "build"
				echo "test"
				echo "integration test"
			}
		}
		stage('integration test') {
			steps {
				echo "build"
				echo "test"
				echo "integration test"
			}
		}
	}
}